version: 2
updates:
    - package-ecosystem: github-actions
      commit-message:
          include: scope
          prefix: ci
      directory: /
      groups:
          github-owned:
              patterns:
                  - "actions/*"
                  - "github/*"
                  - "octokit/*"
      open-pull-requests-limit: 20
      schedule:
          interval: monthly
